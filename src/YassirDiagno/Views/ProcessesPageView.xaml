<UserControl x:Class="YassirDiagno.Views.ProcessesPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="24,18,24,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,12">
            <StackPanel HorizontalAlignment="Left">
                <TextBlock Text="PROCESSUS" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                <TextBlock Text="Top processus consommateurs CPU et RAM (refresh 2s)" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="Trier par :" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <Button Command="{Binding SortByCpuCommand}" Content="CPU" Padding="12,5" FontSize="11" Margin="0,0,4,0"
                        Background="Transparent" Foreground="{DynamicResource PrimaryTextBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Cursor="Hand"/>
                <Button Command="{Binding SortByRamCommand}" Content="RAM" Padding="12,5" FontSize="11" Margin="0,0,4,0"
                        Background="Transparent" Foreground="{DynamicResource PrimaryTextBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Cursor="Hand"/>
                <Button Command="{Binding SortByNameCommand}" Content="Nom" Padding="12,5" FontSize="11" Margin="0,0,8,0"
                        Background="Transparent" Foreground="{DynamicResource PrimaryTextBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Cursor="Hand"/>
                <Button Command="{Binding RefreshNowCommand}" Content="↻ Actualiser" Padding="12,5" FontSize="11"
                        Background="{DynamicResource AccentBlueBrush}" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="{DynamicResource SurfaceBrush}" CornerRadius="6" Padding="14,8" Margin="0,0,0,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="60"/>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="80"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="PID" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TertiaryTextBrush}"/>
                <TextBlock Grid.Column="1" Text="NOM" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TertiaryTextBrush}"/>
                <TextBlock Grid.Column="2" Text="CPU %" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TertiaryTextBrush}" TextAlignment="Right"/>
                <TextBlock Grid.Column="3" Text="RAM (MB)" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TertiaryTextBrush}" TextAlignment="Right"/>
                <TextBlock Grid.Column="4" Text="THREADS" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TertiaryTextBrush}" TextAlignment="Right"/>
                <TextBlock Grid.Column="5" Text="DÉMARRÉ" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TertiaryTextBrush}" TextAlignment="Center"/>
                <TextBlock Grid.Column="6" Text="ACTION" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TertiaryTextBrush}" TextAlignment="Center"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding Processes}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1" Padding="14,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="60"/>
                                        <ColumnDefinition Width="80"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Pid}" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center"/>
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                        <TextBlock Text="{Binding Title}" FontSize="9" Foreground="{DynamicResource TertiaryTextBrush}" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding CpuPercent, StringFormat={}{0:F1}%}" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource AccentBlueBrush}" TextAlignment="Right" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="3" Text="{Binding MemoryMB, StringFormat={}{0:F0} MB}" FontSize="11" FontWeight="SemiBold" Foreground="{DynamicResource AccentGreenBrush}" TextAlignment="Right" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="4" Text="{Binding Threads}" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" TextAlignment="Right" VerticalAlignment="Center"/>
                                    <TextBlock Grid.Column="5" Text="{Binding StartTime}" FontSize="10" Foreground="{DynamicResource TertiaryTextBrush}" TextAlignment="Center" VerticalAlignment="Center"/>
                                    <Button Grid.Column="6" Command="{Binding DataContext.KillCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}"
                                            Content="✕ Tuer" Padding="8,4" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"
                                            Background="{DynamicResource AccentRedLightBrush}" Foreground="{DynamicResource AccentRedBrush}" BorderThickness="0" Cursor="Hand"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock Text="{Binding RefreshStatus}" FontSize="10" Foreground="{DynamicResource AccentGreenBrush}" Margin="14,12,0,0"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
