<UserControl x:Class="YassirDiagno.Views.HardwarePageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource WindowBackgroundBrush}">
    <Grid Margin="24,18,24,18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="MATÉRIEL" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource PrimaryTextBrush}"/>
            <TextBlock Text="Inventaire détaillé + monitoring par cœur" FontSize="11" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,0"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <Border Style="{StaticResource CardStyle}" Margin="0,0,0,10">
                    <StackPanel>
                        <Grid Margin="0,0,0,12">
                            <StackPanel HorizontalAlignment="Left">
                                <TextBlock Text="CPU - DÉTAIL PAR CŒUR" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource TertiaryTextBrush}"/>
                                <TextBlock Text="{Binding CpuName}" FontSize="11" Foreground="{DynamicResource PrimaryTextBrush}" FontWeight="SemiBold" Margin="0,2,0,0"/>
                            </StackPanel>
                            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                                <StackPanel Margin="0,0,16,0">
                                    <TextBlock Text="Charge totale" FontSize="9" Foreground="{DynamicResource TertiaryTextBrush}" HorizontalAlignment="Right"/>
                                    <TextBlock Text="{Binding CpuTotalLoad, StringFormat={}{0:F1}%}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource AccentBlueBrush}" HorizontalAlignment="Right"/>
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="Fréquence moy." FontSize="9" Foreground="{DynamicResource TertiaryTextBrush}" HorizontalAlignment="Right"/>
                                    <TextBlock Text="{Binding CpuAverageClockGhz, StringFormat={}{0:F2} GHz}" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource AccentGreenBrush}" HorizontalAlignment="Right"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding Cores}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="2"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="{DynamicResource SurfaceAltBrush}" CornerRadius="6" Padding="10,8" Margin="3">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="48"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding Index, StringFormat='#{0}'}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center"/>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <Border Background="{DynamicResource BorderBrush}" Height="6" CornerRadius="3" Margin="0,0,0,4">
                                                    <Border HorizontalAlignment="Left" Height="6" CornerRadius="3" Background="{DynamicResource AccentBlueBrush}"
                                                            Width="{Binding LoadPercent, FallbackValue=0}"/>
                                                </Border>
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="{Binding LoadPercent, StringFormat={}{0:F0}%, FallbackValue=--%}" FontSize="10" Foreground="{DynamicResource AccentBlueBrush}" FontWeight="SemiBold"/>
                                                    <TextBlock Text="  ·  " FontSize="10" Foreground="{DynamicResource TertiaryTextBrush}"/>
                                                    <TextBlock Text="{Binding ClockMHz, StringFormat={}{0:F0} MHz, FallbackValue=-- MHz}" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <ItemsControl ItemsSource="{Binding Items}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,8" Padding="16,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="{DynamicResource AccentBlueLightBrush}" CornerRadius="4" Padding="8,3" HorizontalAlignment="Left" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Category}" FontSize="10" FontWeight="Bold" Foreground="{DynamicResource AccentBlueBrush}"/>
                                    </Border>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Name}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource PrimaryTextBrush}"/>
                                        <TextBlock Text="{Binding Detail}" FontSize="10" Foreground="{DynamicResource SecondaryTextBrush}" Margin="0,2,0,0" TextWrapping="Wrap"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
